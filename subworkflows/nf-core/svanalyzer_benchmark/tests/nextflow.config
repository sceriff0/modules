process {
    withName: "BCFTOOLS_ISEC_QUERY" {
        ext.args = "-C --output-type z"
        ext.prefix = {"${meta.id}_tp_comp"}
    }

    withName: "BCFTOOLS_ISEC_TRUTH" {
        ext.args = "-C --output-type z"
        ext.prefix = {"${meta.id}_tp_base"}
    }
}
